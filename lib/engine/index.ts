/**
 * Typecade Engine — barrel export.
 *
 * Pure TypeScript, no framework dependencies.
 */
export { calculateWpm, calculateAccuracy, countCorrectChars } from './scoring';
export type { TypingStats, KeystrokeEvent, WordCompleteEvent } from './types';
